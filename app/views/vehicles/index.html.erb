<article id="contact">
    <div class="formContainer">
<h1> Your Vehicles </h1>
<br />
<br />
<% @vehicles.each do |vehicle| %>
    <h3><%= link_to vehicle.make, vehicle_path(vehicle)%> - <%= link_to vehicle.model, vehicle_path(vehicle) %></h3>
    <br />
<% end %>


<%= link_to("Add a vehicle", new_vehicle_path) %>
</div>
</article>



<!-- create_table "vehicles", force: :cascade do |t|
  t.string "make"
  t.string "model"
  t.integer "year"
  t.string "vin"
  t.string "engine"
  t.string "car_classification"
  t.string "color"
  t.datetime "created_at", null: false
  t.datetime "updated_at", null: false
end -->
