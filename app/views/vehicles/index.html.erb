<h1>Vehicles Index Page</h1>

<% @vehicles.each do |vehicle| %>
    <li><%= link_to vehicle.model, vehicle_path(vehicle) %></li>
    <br />
<% end %>

<%= link_to("Add a vehicle", new_vehicle_path) %>



<!-- create_table "vehicles", force: :cascade do |t|
  t.string "make"
  t.string "model"
  t.integer "year"
  t.string "vin"
  t.string "engine"
  t.string "car_classification"
  t.string "color"
  t.datetime "created_at", null: false
  t.datetime "updated_at", null: false
end -->
